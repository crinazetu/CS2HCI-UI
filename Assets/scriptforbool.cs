using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scriptforbool : MonoBehaviour
{
    public static bool ShowItms;
    // Start is called before the first frame update
    public void OnClickConfirm(){
        ShowItms = true;
    }
}
